// This is the new action engine that I'm writing to handle all the crap that
// the Mobs and such can do and will be able to do.

function actionEngine() {
    //placeholder
    this.init_();



}

actionEngine.prototype.init_ = function() {
    //placeholder
};

//actEngine.prototype._init = function() {
    //placeholder
    //this._listeners = {};
    //this._ignored = {};
//};

// As it turns out, I don't need an event handler if I'm not handling operations
// asynchronously. The actEngine as I see it at this point is basically going
// to handle our Mob actions by encapsulating the logic for their basic actions:
// things like moving, dying, updating statuses, combat, etc.





//actEngine.prototype.hear = function() {
    //placeholder
//};

//actEngine.prototype.listen = function(key, listener) {
    //placeholder
    //this._listeners[key] = listener;
//};

//actEngine.prototype.ignore = function(key) {
    //placeholder
    //var clone = {};
    //for (var oldKey in this._listeners) {
        //if (oldKey !== key) {
            //clone[oldKey] = this._listeners[oldKey];
        //}
    //}
    //this._listeners = clone;
//};

//actEngine.prototype.relay = function() {
    //placeholder
//};

